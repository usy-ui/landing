"use client";
import { Scrollable, usySpacing } from "@usy-ui/base";
import styled from "styled-components";

export const StyledScrollable = styled(Scrollable)`
  border-radius: ${usySpacing.px6};
`;
